#include "InputHandler.h"

const int MAX_INPUTMAP_SIZE = 32;
namespace Ceres
{
    InputHandler::InputHandler()
        // :_map(std::unordered_map<unsigned int, std::function<void>>(MAX_INPUTMAP_SIZE))
    {}
}